{
  "name": "subpath-import-example",
  "version": "0.0.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "start": "rm -rf dist && tsc --build && chmod a+x ./dist/main.js && ./dist/main.js"
  },
  "imports": {
    "#stuff/*.js": "./dist/stuff/*.js"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
